    本程序使用最简单的方式直接将源文件写入映像。没有文件系统，直接覆盖映像相应位置处的数据，所以源文件和映像可以是任意的格式。扇区数从1开始计数，比如将一个512字节的文件写入映像的1扇区，那么就是将源文件写入映像文件的0-511字节。写入的时候会自动判断MZ文件头，并去掉。判断MZ文件头通过第0，1字节的“MZ”标志和最后1扇区字节数进行。“1.44M映像”是将映像大小变为标准的1440 * 1024字节，因为VPC不让加载非标准大小的映像，所以提供此功能。
    内含一个自己写的超链接类，如果你和我一样用VC6，也许会有用。
    就这么多了，希望对你有用。界面很丑，功能很简单，写的很烂，请尽情鄙视。